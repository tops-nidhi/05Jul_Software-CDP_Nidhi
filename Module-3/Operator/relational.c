#include<stdio.h>
void main()
{
    int a,b;
    printf("Enter the value of 2 no:");
    scanf("%d%d",&a,&b);
    // a=14, b=12
    if (a != b)
    {
        printf("A and B are not equle.");
    }
    else
    {
        printf("A and B both are equle.");
    }
    
}